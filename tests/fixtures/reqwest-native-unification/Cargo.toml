[package]
name = "reqwest-native-unification-check"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
openbao = { path = "../../..", version = "=2.0.1", default-features = false, features = ["rustls-tls"] }
reqwest = { version = "0.13.4", default-features = false, features = ["native-tls"] }
